# Transformations

Generated by `python -m src.anonymise`. Do not edit by hand.

## Parameters

| parameter | value |
| --- | --- |
| `aggregate_min_subscribers_per_cell` | `10` |
| `area_mode` | `enb_tokenised` |
| `dp_epsilon` | `1.0` |
| `k` | `10` |
| `min_subscribers_per_area` | `10` |
| `numeric_round_sig_figs` | `3` |
| `numeric_winsorise` | `[0.01, 0.99]` |
| `rare_category_min_subscribers` | `10` |
| `time_bucket_minutes` | `15` |
| `volume_top_code_quantile` | `0.99` |

## Transformations applied

| output column | source column | transformation | parameters | rationale | rows affected |
| --- | --- | --- | --- | --- | --- |
| (removed) | msisdn | dropped | - | Direct identifier, removed before any release. | 1,099,340 |
| (removed) | imsi | dropped | - | Direct identifier, removed before any release. | 1,099,340 |
| (removed) | imei | dropped | - | Direct identifier, removed before any release. | 1,099,340 |
| (removed) | tac | dropped | - | Direct identifier; on real data tac would become a device class. | 1,099,340 |
| time_bucket | time_start | floored to time_bucket | time_bucket_minutes=15, native_window_minutes=10 | Coarsens the timestamp so a cell visit is not pinpointed to the second. | 1,099,340 |
| area | enb | rare cells folded into <province>_OTHER | min_subscribers_per_area=10, n_rare_cells=99 | A cell with few subscribers is close to an identifier on its own. | 484 |
| area | enb | remaining cells replaced by random tokens | area_mode=enb_tokenised, n_tokens=1884 | Token order is random and the map is never persisted. | 1,098,856 |
| radio_access_type | radio_access_type | no rare categories | min_subscribers=10 | - | 0 |
| application_category | application_category | no rare categories | min_subscribers=10 | - | 0 |
| tp_dl_avg | tp_dl_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 17,752 |
| tp_ul_avg | tp_ul_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 16,818 |
| tp_dl_filtered_avg | tp_dl_filtered_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 2,386 |
| cont_rtt_radio_avg | cont_rtt_radio_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 11,812 |
| cont_rtt_internet_avg | cont_rtt_internet_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 12,504 |
| initial_rtt_radio_avg | initial_rtt_radio_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 15,082 |
| tcp_retrans_byte_ratio_downlink_avg | tcp_retrans_byte_ratio_downlink_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 21,004 |
| tcp_retrans_byte_ratio_uplink_avg | tcp_retrans_byte_ratio_uplink_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 21,980 |
| http_response_time_avg | http_response_time_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 10,994 |
| http_sr_avg | http_sr_avg | winsorised and rounded | lower_quantile=0.01, upper_quantile=0.99, significant_figures=3 | Extreme values are rare enough to single a subscriber out. | 631 |
| data_GB_sum | data_GB_sum | top-coded and rounded | quantile=0.99, significant_figures=3 | Heavy users are outliers and are identifiable by volume alone. | 10,994 |
| im_video_GB_sum | im_video_GB_sum | top-coded and rounded | quantile=0.99, significant_figures=3 | Heavy users are outliers and are identifiable by volume alone. | 3,417 |
| im_audio_GB_sum | im_audio_GB_sum | top-coded and rounded | quantile=0.99, significant_figures=3 | Heavy users are outliers and are identifiable by volume alone. | 10,994 |
| tethering_data_GB_dl_sum | tethering_data_GB_dl_sum | top-coded and rounded | quantile=0.99, significant_figures=3 | Heavy users are outliers and are identifiable by volume alone. | 10,994 |
| area | area | escalated to province for groups below k | k=10, qi_columns=['time_bucket', 'area', 'radio_access_type', 'application_category'] | Coarsening location rescues rows that would otherwise be dropped. | 369,536 |
| time_bucket, area, radio_access_type, application_category | time_bucket, area, radio_access_type, application_category | rows suppressed for still being below k | k=10 | Last resort once generalisation cannot reach k distinct subscribers. | 3,533 |
| (removed) | _subscriber | internal key dropped before write | - | Never released. | 1,095,807 |

## Suppressed rows

- **aggregate**: 1,111 of 3,753 cells suppressed (29.603%) - 1,033 primary (below the subscriber minimum) and 78 secondary (smallest survivor in an affected slice).
- **aggregate noise**: Laplace, epsilon 1.0, scale 1.0, seed 42, applied to n_subscribers, n_rows.
- **record**: 3,533 rows suppressed (0.321375%), 369,536 rows escalated to province (33.6144%); min 10 distinct subscribers per QI group.

## Assumptions and limitations

- The extract covers a single hour on one date, so no cross-day linkage is
  possible here and longitudinal risk is untested.
- `tac` is constant across every subscriber in this extract, so device-model
  risk cannot be measured from it.
- Area tokens are assigned under seed 42 for reproducibility. Anyone holding
  both the raw extract and this code can rebuild the token map, so
  tokenisation protects an external recipient, not Elisa as the source holder.
- The session key is ephemeral and destroyed after use, so `session_id` cannot
  be re-derived, but it does link a subscriber's rows within the release.
- Aggregates are published at one granularity only. No marginals or slice
  totals are released, and within any slice that lost a cell the smallest
  survivor is suppressed too, so a suppressed cell cannot be differenced out.
- The Laplace scale assumes sensitivity 1. That holds for `n_subscribers`;
  `n_rows` has a higher user-level sensitivity, so the stated epsilon is not
  a strict user-level guarantee for that column.
- Which cells are suppressed is decided on true counts, so the published
  cell set is not itself covered by epsilon.
