# Release parameters for the anonymisation step (M2).
# k counts DISTINCT SUBSCRIBERS per QI group, never rows.

k: 10                        # DISTINCT SUBSCRIBERS per QI group, not rows
time_bucket_minutes: 15      # use the native window if it is coarser
area_mode: enb_tokenised     # enb_tokenised | province
min_subscribers_per_area: 10
rare_category_min_subscribers: 10
numeric_winsorise: [0.01, 0.99]
numeric_round_sig_figs: 3
volume_top_code_quantile: 0.99
aggregate_min_subscribers_per_cell: 10
dp_epsilon: 1.0              # Laplace noise (seed 42) on n_subscribers
# Contribution bound: the most aggregate cells one subscriber may influence.
# Noise scale is max_cells_per_subscriber / dp_epsilon. Persisted here (not just
# computed at runtime) so every module reads the same value - src/evaluate.py
# silently assumed 1 and understated the scale 11-fold until this was added.
max_cells_per_subscriber: 11
